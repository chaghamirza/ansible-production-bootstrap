server = "https://registry-1.docker.io"

[host."https://mirror-docker.runflare.com"]
  capabilities = ["pull", "resolve"]

[host."https://docker-mirror.liara.ir"]
  capabilities = ["pull", "resolve"]
